interface:
  display_name: "Env Report"
  short_description: "Review survey data and generate occupational sampling plans"
  default_prompt: "Use $env-report to parse and review this workplace survey PDF, validate normalized data, and generate the sampling-plan DOCX."
